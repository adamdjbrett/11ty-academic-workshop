{
  "name": "academic-workshop-11ty-port",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:css && rm -rf _site && eleventy --config=eleventy.config.js",
    "start": "npm run build:css && eleventy --serve --config=eleventy.config.js --incremental",
    "build:css": "node scripts/build-css.mjs"
  },
  "engines": {
    "node": ">=24",
    "npm": ">=11.14.0"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@11ty/eleventy-plugin-rss": "^2.0.2",
    "luxon": "^3.5.0",
    "sass": "^1.86.3"
  },
  "packageManager": "npm@11.14.0"
}
